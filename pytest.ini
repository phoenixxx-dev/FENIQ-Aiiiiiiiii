[pytest]
asyncio_mode = strict
testpaths = apps/api/tests packages/data-engine/tests
markers =
    slow: اختبارات ثقيلة (ملفات ضخمة) — تُستثنى من التشغيل الافتراضي
# اختبار نصف المليون صف يستغرق دقيقة ويولّد 30 ميغابايت؛ يُشغَّل عند الطلب:
#   pytest -m slow
addopts = -m "not slow"
